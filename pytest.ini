[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests that require network access and a real FastF1 API call (deselect with '-m "not integration"')

# Ensure project root is on PYTHONPATH so imports work without pip install -e
pythonpath = .
